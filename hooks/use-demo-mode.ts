/**
 * DEPRECATED: Demo mode has been removed.
 * This stub exists only for backward compatibility.
 * All users now use the same production mode with scenario templates.
 */

export function useDemoMode() {
  return {
    isDemo: false,
    user: null
  }
}
